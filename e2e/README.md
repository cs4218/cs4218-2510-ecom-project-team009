Admin: admin@playwright.com / AdminPass123!
User: user@playwright.com / UserPass123!

Run:

```
MONGO_URL=mongodb://127.0.0.1:27017/ecom_e2e npm run test:e2e
```

Storage states are not used; tests should log in via UI or call login API.
